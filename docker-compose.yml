version: "2"

services:
  core:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4000:4000"

  mongodb:
    image: mongo:latest
    ports:
      - 27017:27017
  rabbitmq:
    image: rabbitmq:3.5.3-management
    ports:
      - 5672:5672
      - 15672:15672
